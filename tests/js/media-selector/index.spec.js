describe( 'media-selector', () => {
	describe( 'Post Media Frame', () => {
		it( 'should be overridden', () => {
			expect( true ).toBe( true );
		} );
	} );

	describe( 'Select Media Frame', () => {
		it( 'should be overridden', () => {
			expect( true ).toBe( true );
		} );
	} );
} );
