{
  "name": "@xwp/unsplash-wp",
  "version": "1.0.0",
  "description": "Unsplash for WordPress.",
  "author": "XWP",
  "license": "GPL-2.0-or-later",
  "bugs": {
    "url": "https://github.com/xwp/unsplash-wp/issues"
  },
  "homepage": "https://github.com/xwp/unsplash-wp#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xwp/unsplash-wp.git"
  },
  "scripts": {
    "postinstall": "composer install",
    "dev": "wp-scripts start",
    "build": "wp-scripts build",
    "lint": "npm-run-all --parallel lint:*",
    "lint:js": "eslint js/src",
    "lint:php": "composer lint",
    "test": "npm-run-all --parallel test:*",
    "test:php": "npm-run-all test:php:unit test:php:integration",
    "test:php:unit": "composer test",
    "test:php:integration": "composer test -- -c phpunit.integration.xml",
    "test:php:coverage": "npm-run-all test:php:coverage:unit test:php:coverage:integration",
    "test:php:coverage:unit": "composer test -- --coverage-clover tests/reports/unit-clover.xml",
    "test:php:coverage:integration": "composer test -- -c phpunit.integration.xml --coverage-clover tests/reports/integration-clover.xml",
    "test:php:coverage:merge": "composer coverage-merge",
    "coverage": "npm-run-all --parallel coverage:*",
    "coverage:php": "composer coverage",
    "docker": "docker-compose run --workdir=/var/www/html/wp-content/plugins/unsplash-wp wordpress"
  },
  "devDependencies": {
    "@wordpress/block-editor": "3.3.0",
    "@wordpress/eslint-plugin": "3.2.0",
    "@wordpress/scripts": "6.0.0",
    "eslint": "6.7.2",
    "npm-run-all": "4.1.5"
  },
  "dependencies": {}
}
